import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const primary = Color(0xFF007AFF);
  static const primarySoft = Color(0xFF5AC8FA);
  // NOTE: these were previously named after colors they did not actually
  // render as (e.g. `lime` was a near-white blue). Values below now match
  // their names so the palette can be reasoned about by name alone.
  static const lime = Color(0xFFD3F26A);
  static const green = Color(0xFF34C759);
  static const ink = Color(0xFF111827);
  static const muted = Color(0xFF6B7280);
  static const surface = Color(0xFFF5F7FB);
  static const card = Colors.white;
  static const stroke = Color(0xFFE5E7EB);
  static const darkSurface = Color(0xFF05070B);
  static const darkCard = Color(0xFF111318);
  static const darkStroke = Color(0xFF2A2F3A);
  static const yellow = Color(0xFFFFE29A);
  static const pink = Color(0xFFFFD1DE);
  static const blue = Color(0xFFBEE1FF);
  static const orange = Color(0xFFFFC38C);
}
